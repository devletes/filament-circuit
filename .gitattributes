# Stored LF, checked out LF everywhere — the shipped JS and CSS are read
# by browsers and diffed by contributors on every platform.
* text=auto eol=lf

/.gitattributes   export-ignore
/.gitignore       export-ignore
/.github          export-ignore
/tests            export-ignore
/phpunit.xml      export-ignore
